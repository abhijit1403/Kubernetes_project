- hosts: all
  become: true
  tasks:
     - name: create new deployment
       command: kebectl apply -f /home/jenkins/Deployment.yml
     - name: create new service
       command: kubectl apply -f /home/jenkins/servies.yml