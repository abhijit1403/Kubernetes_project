- hosts: all
  become: true
  tasks:
     - name: create new deployment
       command: kubectl apply --validate=false -f /home/jenkins/deployment.yml
     - name: create new service
       command: kubectl apply --validate=false -f /home/jenkins/Services.yml